# FinBot v2 — variáveis de produção (copiar para .env na VPS)
# cp .env.example .env && nano .env

DB_USER=crypto_admin
DB_PASSWORD=changeme
DB_NAME=crypto_bot

BINANCE_API_KEY=
BINANCE_API_SECRET=

DRY_RUN=false
TZ=America/Sao_Paulo

UPDATE_INTERVAL=900
MAX_POSITIONS=20
MAX_HOLD_HOURS=24
MONITOR_INTERVAL=10

FUTURES_ENABLED=true
